# 브동산 — Claude Code 작업 지침

이 저장소에서 작업을 시작하기 전에 **[claude/handoff-master.md](claude/handoff-master.md)를 반드시 전체 읽을 것.** 디자인 기준, 콘텐츠 제작 기준(부동산/브랜드), 적정가 산출법("브동산 모형"), 지도 어노테이션 절차, IT/배포 기준, 반복 실수 노트 등 모든 업무 기준이 그 문서 하나에 정리돼 있다. 이 문서는 그 위에 **Claude Code(로컬 세션) 전용으로 달라지는 부분만** 보충한다.

## Claude Code 세션에서 달라지는 점 (master 문서와의 차이, 2026.09.12 확인)

master 문서는 claude.ai 클라우드 채팅 세션 기준으로 작성돼 일부 제약이 있는데, 이 저장소를 직접 다루는 Claude Code 로컬 세션에서는 아래 두 가지가 다르다.

1. **이미지 캡처**: master 3-5는 "Claude가 클라우드 세션에서 임의 웹사이트 이미지를 직접 다운로드/캡처할 수 없다"고 되어 있으나, Claude Code에는 브라우저 도구(`mcp__Claude_Browser__*`)가 있어 공식 홈페이지에 직접 접속해 스크린샷/확대 캡처가 가능하다. Paul과 합의된 방식은 **파일 다운로드보다 스크린샷 캡처 우선** — 매번 다운로드 승인을 받는 대신 화면 캡처로 필요한 영역(조감도·평면도·위치도 등)을 가져와 활용한다.
2. **GitHub 배포**: master 7-1은 "GitHub 자동 업로드는 클라우드 세션에서 불가능(push 차단)"이라고 되어 있으나, 이 로컬 저장소는 `origin`(`zzanghyuny/brond`, main)에 대한 push 권한이 확인됐다(2026.09.12, `git push --dry-run` 성공). 즉 **이 세션에서는 완성된 파일을 직접 커밋·푸시해 배포할 수 있다** — Paul이 GitHub 웹 UI로 수동 업로드할 필요 없이, 커밋 전 반드시 변경 파일과 커밋 메시지를 Paul에게 보여주고 확인받은 뒤 진행한다(일반 git 안전수칙 동일 적용). 새 포스팅 등록 시 함께 커밋해야 하는 파일은 master 7-2-A 참고: 포스팅 HTML + `realestate.html`/`brand.html`(카드 직접 추가) + `sitemap.xml`. (`posts-data.js`/`feed.js` 피드 시스템은 설계만 있고 미구현 — 2026.09.12 정정.)
3. **이미지 파일 형식**: master 3-2는 "Artifact CSP 때문에 data: URI(base64) 인라인 임베드 필수"라고 되어 있으나, 이는 Artifact 미리보기 환경의 제약이다. 이 저장소는 GitHub+Vercel로 직접 서빙되는 정적 사이트라 이 제약이 없다 — **포스팅 이미지는 `posts/images/<포스팅명>/` 아래 별도 파일로 저장하고 상대경로 `<img src="images/...">`로 참조한다.** base64로 인라인 임베드하면 HTML 파일이 수 MB로 불어나(기존 1·2호 포스팅이 이 방식이라 파일당 2~5MB) git diff·로딩 속도에 불리하므로, 새 포스팅부터는 별도 파일 방식을 기본으로 한다. `style="width:100%;display:block;"` 인라인 스타일은 형식과 무관하게 계속 필수(master 3-2 규칙 그대로 유지).
4. **이미지 처리 도구 없음**: 이 로컬 PC(Paul 컴퓨터)에는 Python/PIL, ImageMagick, Node.js가 설치돼 있지 않다(2026.09.12 확인, Windows Store 스텁만 존재). master 3-2·4-4가 전제하는 "bash+PIL 리사이즈·압축", "PIL+numpy 지도 색 채널 탐지" 절차를 이 세션에서는 그대로 실행할 수 없다. 대안: (a) 이미지 리사이즈·압축 없이 원본 그대로 사용(공식 홈페이지 이미지는 대체로 1~2MB대라 치명적이지 않음), (b) Paul이 마커 찍어준 실제 지도 스크린샷을 그대로 가공하는 대신, 지도에서 관찰한 상대 위치를 바탕으로 **브동산 자체 제작 SVG 개략도**(코드로 직접 좌표 지정)를 그려 대체한다 — 색 규칙(SITE=빨강, 비교단지=초록, 배정초=파랑)은 동일하게 적용하되 "브동산 자체 제작 개략도"임을 캡션에 명시. Paul이 Python 설치를 원하면 그때 다시 PIL 기반 절차로 전환.

## 새 포스팅(부동산) 작업 시작 시 체크
- master 4-6에 따라 필요한 사진(대표사진/교통망·위치도/조감도·배치도·평면도/적정가용 지도)을 Paul에게 요청되어 있는지 확인.
- master 4-3의 적정가 산출 절차, 4-4의 지도 마킹 색 규칙(SITE=빨강, 비교단지=초록, 배정초등학교=파랑)을 그대로 따를 것.
- 작업 중 master 문서와 실제 최신 게시물([posts/](posts/)) 코드가 다르면 실제 코드 쪽을 우선 참고하고 master 문서 업데이트가 필요한지 Paul에게 확인(master 10-1 교훈).
